import routes from './routes';

const config = {
    routes, // <=> routes:routes;
};
export default config; //do export default nên khi import thì lấy thẳng hoặc đổi tên tùy ý chứ khi export lẻ nhiều thì mới dùng dấu {}
